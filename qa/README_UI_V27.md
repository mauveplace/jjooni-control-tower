# UI V27

This revision improves wide/tablet trade readability, upgrades ticker-placeholder names when later human names are available, suppresses the mobile More menu on wide layouts, and fails closed on unverified billing currency. It does not change trading or broker execution logic.
